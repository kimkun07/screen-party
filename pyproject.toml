# Root configuration for development tools only
# This file is not used for building packages

[tool.black]
line-length = 100
target-version = ['py313']

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["server/tests", "client/tests"]

[tool.pyright]
pythonVersion = "3.13"
